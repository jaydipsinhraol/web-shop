import { HttpExceptionFilterFilter } from './http-exception.filter.filter';

describe('HttpExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new HttpExceptionFilterFilter()).toBeDefined();
  });
});
